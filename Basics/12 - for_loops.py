numbers = [1, 2, 3, 4, 5]

# in each iteration item = numbers[i], it means that item will be the next element of the numbers till the the end of the list, like in a while but it is a shorter code
for item in numbers:
    print(item)
